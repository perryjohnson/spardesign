everything has been recently changed to use TrueGrid for mesh generation
...updates to follow shortly

for now, all the development files are in the `truegrid` directory

old mayavi-specific gridgen files have been moved to the `deprecated/autogridgen` directory