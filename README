The biplane spars in this branch do not have a root region or a root transition region.

(0) identify a layup file to use in truegrid\writeTGscripts.py and main.py

(1) write TrueGrid scripts
(from iPython):
  |>cd github\spardesign\truegrid
  |>run writeTGscripts

(2) run TrueGrid scripts and write ABAQUS output files
(from Admin Windows Command Prompt):
  >cd %truegrid%
  >alltg

(3) translate ABAQUS output files to VABS input files and run VABS
(from iPython ... all 24 stations take about 11 minutes to run):
  |>cd ..
  |>run main

(4) move the VABS output files to a new folder outside the Dropbox,
    named for today's date (e.g. YYYYMMDD = 20120305)
(from Cygwin):
  $ mkdir "/cygdrive/d/data/YYYY-MM-DD (description)"
  $ mv spar_station_* "/cygdrive/d/data/YYYY-MM-DD (description)"

(5) copy each of the *.dat.K files (mass and stiffness matrices)
    back inside the VABS folder
(from Cygwin):
  $ cp "/cygdrive/d/data/YYYY-MM-DD (description)/*.dat.K VABS/M_and_K_matrices"

(6) move the TrueGrid output files to the Desktop
(from Cygwin):
  $ mkdir "/cygdrive/d/data/YYYY-MM-DD (description)/truegrid"
  $ mkdir "/cygdrive/d/data/YYYY-MM-DD (description)/truegrid/abaqus_output_files"
  $ mv truegrid/spar_station_*_abq.txt "/cygdrive/d/data/YYYY-MM-DD (description)/truegrid/abaqus_output_files"

(7) copy the TrueGrid input files to the Desktop
(from Cygwin):
  $ mkdir "/cygdrive/d/data/YYYY-MM-DD (description)/truegrid/truegrid_input_files"
  $ cp truegrid/*.tg "/cygdrive/d/data/YYYY-MM-DD (description)/truegrid/truegrid_input_files"

(8) translate all the VABS output *.dat.K files
    to a code block (dymoreMKblock.dat) for a DYMORE input file
(from iPython):
  |>run writeDYMOREfiles

(9) copy and paste the contents of dymoreMKblock.dat
    to the DYMORE input file (monoplane_spar.dat, lines 174-532)
(from mouse & keyboard):
  ...

(10) copy dymoreMKblock.dat to the Desktop
  $ mkdir "/cygdrive/d/data/YYYY-MM-DD (description)/dymore"
  $ cp dymoreMKblock.dat "/cygdrive/d/data/YYYY-MM-DD (description)/dymore"

(11) run DYMORE
(from Windows Command Prompt):
  >cd %spardesign%
  >cd monoplane_spar_constload\[un]twisted\[RESTOFPATH]
  >clean
  >rundymore

(12) fix the bug in the GnuPlot script generated by DYMORE
     (monoplane_sparPlotObjPNG.plt, line 86)
(from mouse & keyboard):
  replace ", \" with ";"

(13) generate PNG plots of DYMORE results
(from Windows Command Prompt):
  >monoplane_sparPlotObjPNG.plt
  >monoplane_sparPlotSvyPNG.plt

(14) get the tip deflection from the DYMORE results
(see svy_disp_spar.mdt, column 4, last row)

-------------------------------------------------------------------------------

Notes:
------
(1) PYTHON must already be installed on your system
(2) the "license" file for VABS must be in the top directory (spardesign)
    in order for VABS to run correctly
(3) TrueGrid must already be installed on your system

Other remarks:
--------------
(1) everything has been recently changed to use TrueGrid for mesh generation
    for now, all the development files are in the "truegrid" directory
(2) old Mayavi-specific gridgen files have been moved to the
    "deprecated/autogridgen" directory